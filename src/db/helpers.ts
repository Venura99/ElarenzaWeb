// Prisma generated ids and maintained updatedAt automatically; with Drizzle
// both are set explicitly at the call site.
export function newId() {
  return crypto.randomUUID();
}

export function now() {
  return new Date();
}
